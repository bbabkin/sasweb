- @page_title = "New Article"
= link_to("Back to News Article List", {:action => 'index'}, :class => 'btn btn-xs btn-default')
= form_for [:admin, @newsitem], :html => {:multipart => true} do |f|
	= render(:partial => "form", :locals => {:f => f})
	.form-buttons
		= submit_tag("Create News Article", :class => 'btn btn-xs btn-default')
